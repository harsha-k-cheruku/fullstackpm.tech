# Pipeline-only deps (NOT installed on Render)
# Render uses code/requirements.txt — keep these separate to keep prod slim.
#
# Install: pip install -r pipeline/requirements.txt
#
# All other deps come from code/requirements.txt (anthropic, feedparser, sqlalchemy, etc.)

trafilatura>=1.12.0
lxml>=5.0.0
